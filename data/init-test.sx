;;
; vim:ft=lisp
;;

(on-event power-on

  (keep-alive "kyu-server-configuration" "data/configuration/.*\\.sx")

  (keep-alive "ipc-hub")

  (keep-alive "kyu-server-job"))

(on-event ctrl-alt-del
  (power-reset))

(on-event power-reset
  (run #f "echo" "power-reset, nao"))

(on-event power-down
  (run #f "echo" "power-down, nao"))
