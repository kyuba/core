;;
; vim:ft=lisp
;;

(daemon s-udev "UDEV daemon"
  (provides "udev" "fs-udev")
  (requires "fs-proc" "fs-sys")
  (before ".*")
  (schedule-limitations once-per-host)
  (functions 
    (start
      (run "echo" "udev starting..."))
    (stop
      (run "echo" "udev stopping..."))))

